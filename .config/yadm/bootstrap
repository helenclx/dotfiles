#!/usr/bin/env bash

echo "Change Bash configuration to the one in my yadm repo..."
yadm restore .bashrc

echo "Install additional packages that use these dot files..."
sudo dnf install -y zsh zoxide vim fcitx5 fcitx5-chinese-addons easyeffects ghostwriter
